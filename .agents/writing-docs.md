# Writing Docs

- Use the terms from `CONTEXT.md`.
- Keep public docs focused on problem, workflow, outcome, requirements, and
  approval gates.
- Do not include private setup values, private paths, screenshots, account data,
  runtime state, message text, or customer/employer data.
- Keep package paths ASCII. Use Thai category names only as display labels.
