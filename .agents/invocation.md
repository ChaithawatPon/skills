# Invocation

Use this catalog when a user wants one of Pon's public-safe agent workflows.

Install the full catalog:

```bash
npx skills add ChaithawatPon/skills
```

Or copy one package directory from `skills/<category>/<name>/`.

Before release, run:

```bash
npm test
```
